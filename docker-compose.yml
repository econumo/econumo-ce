services:
  econumo:
    image: econumo/econumo-ce:latest
    env_file:
      - .env
    entrypoint: [ "/bin/sh", "-c", "/entrypoint.sh" ]
    ports:
      - "8181:80"
    volumes:
      - db:/var/www/db
    restart: unless-stopped

volumes:
  db:
